# 02-stateless-vs-stateful.md

> StatelessKieSession (one-shot) vs KieSession (persistent facts) [sequenceDiagram stateless: insert→fire→discard vs stateful: insert→fire→modify→fire] + interview Q&A
